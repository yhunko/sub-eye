// Committed stand-in for Expo's generated expo-env.d.ts (which is gitignored):
// pulls in process.env.EXPO_PUBLIC_* typing and native asset module declarations.
/// <reference types="expo/types" />
