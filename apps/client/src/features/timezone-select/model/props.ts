export type SharedProps = {
  disabled?: boolean;
  placeholder?: string;
  emptyTitle?: string;
};

export type TimezoneOption = {
  value: string;
  label: string;
  offset: string;
  abbrev: string;
  group: string;
};
