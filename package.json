{
  "name": "sub-eye",
  "version": "3.2.0-beta.10",
  "author": "Yehor Hunko",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "bun-types": "^1.3.9",
    "eslint": "9.39.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "semantic-release": "^25.0.3",
    "syncpack": "^14.0.0-alpha.41",
    "turbo": "^2.8.10",
    "wrangler": "^4.61.1"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "description": "A monorepo template for SubEye subscriptions management app",
  "homepage": "https://github.com/yhunko/sub-eye",
  "keywords": [
    "bun",
    "hono",
    "react",
    "vite",
    "monorepo",
    "turbo"
  ],
  "license": "MIT",
  "packageManager": "bun@1.3.9",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=client",
    "dev:server": "turbo dev --filter=server",
    "build": "turbo build",
    "build:client": "turbo build --filter=client",
    "build:server": "turbo build --filter=server",
    "lint": "turbo lint",
    "react-doctor": "bun x react-doctor@latest . --yes --project client --diff --no-ami --offline --fail-on warning --verbose",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "postinstall": "turbo build --filter=shared --filter=server",
    "prepare": "husky",
    "deploy:dev": "turbo build && wrangler -c dev.wrangler.jsonc deploy --minify"
  },
  "workspaces": [
    "./server",
    "./client",
    "./shared"
  ]
}
